---
# Config for garnix.io builds & caching
builds:
  exclude:
    - '*.cosmic.*'
  include:
    - homeConfigurations.*
    - nixosConfigurations.*
    - packages.x86_64-linux.*
    - packages.aarch64-linux.*
